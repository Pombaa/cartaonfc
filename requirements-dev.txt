# Dev-only. Nenhuma dessas dependências é usada em runtime (o site publicado
# é HTML/CSS/JS vanilla, sem build). Necessárias só para tools/sync.py
# (Pillow, vobject) e tools/verify.py (playwright, vobject).
Pillow==12.3.0
playwright==1.63.0
vobject==0.9.9
